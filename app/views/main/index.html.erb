<div class="d-flex align-items-center justify-content-center">
  <h1>Home Page</h1>

 
</div>

 <% if @user %>
    <h2>Logged in as: <%= @user.email %>
    <%= button_to "logout", logout_path, method: :delete, class: "btn btn-danger" %>
  <% end %>
